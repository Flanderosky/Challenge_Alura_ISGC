version: '3.8'

services:
  alura-agente:
    build: .
    container_name: alura-agente
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
